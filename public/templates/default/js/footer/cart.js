/** * Created by Marjan on 15.06.2016. */$(".cart-header").click(function(){    var width = $(window).width();    if(width >= 400)    {        width = 400;    }    var widthstring = "-"+width+"px";    $(".cart-container").css("width",width+"px");    if($(".cart-container").hasClass("opened"))    {        $("#overlay").fadeOut();        $(".cart-container").removeClass("opened");        $(".cart-container").animate({right: widthstring});    }    else    {        $("#overlay").fadeTo(500,0.2);        $(".cart-container").addClass("opened");        $(".cart-container").animate({right:"0px"});    }});function AnimateRotate(angle) {    // caching the object for performance reasons    var $elem = $('.cart-container');    // we use a pseudo object for the animation    // (starts from `0` to `angle`), you can name it as you want    $({deg: 0}).animate({deg: angle}, {        duration: 2000,        step: function(now) {            // in the step-callback (that is fired each step of the animation),            // you can use the `now` paramter which contains the current            // animation-position (`0` up to `angle`)            $elem.css({                transform: 'rotate(' + now + 'deg)'            });        }    });}